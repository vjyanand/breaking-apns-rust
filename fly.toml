app = 'rust-learn'
primary_region = 'sjc'

[build]

[http_service]
  internal_port = 9090
  force_https = false
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[services]]
  internal_port = 9090
  protocol = "tcp"
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0
  [[services.ports]]
    handlers = ["http"]
    port = 9090
    force_https = false
    [services.ports.http_options]
      idle_timeout = 30

[[vm]]
  memory = '512mb'
  cpu_kind = 'shared'
  cpus = 2
