[package]
name = "rust-breaking"
version = "0.1.7"
edition = "2024"

[dependencies]
hyper = { version = "1.11.1", features = ["client", "http2"] }
env_logger = "0.11.11"
hyper-rustls = { version = "0.27.9", features = ["http2"] }
futures-util = "0.3.34"
http-body-util = "0.1.5"
hyper-util = { version = "0.1.20", features = ["http2", "client"] }
jsonwebtoken = { version = "11.0.0", default-features = false, features = [
  "aws_lc_rs",
  "use_pem",
] }
log = "0.4.34"
serde = "1.0.229"
serde_json = "1.0.151"
sqlx = { version = "0.9.0", features = [
  "postgres",
  "uuid",
  "chrono",
  "runtime-tokio",
] }
tokio = { version = "1.49.0", features = ["full"] }
uuid = { version = "1.19.0", features = ["v4"] }
warp = { version = "0.4.2", features = ["server"] }

[[bin]]
name = "serverAPNS"
path = "src/main.rs"

[profile.release]
opt-level = 3     # Maximum optimization for performance
lto = "fat"       # Enable full Link-Time Optimization
codegen-units = 1 # Minimize binary size with single codegen unit
strip = true      # Strip only debug symbols
panic = "abort"   # Use unwinding for panic handling
#incremental = false
#debug = true
